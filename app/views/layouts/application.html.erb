<!DOCTYPE html>
<html>
  <head>
    <title>INOceaner</title>
	<link href='//fonts.googleapis.com/css?family=Raleway:400,700' rel='stylesheet' type='text/css'>
	<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header id="main_navigation" class="clearfix">
    <%= link_to "INOceaner", root_path, class: "name" %>
 		
      <% if !current_user %>
        <p><%= link_to("註冊", new_user_registration_path) %> 
           <%= link_to("登入", new_user_session_path) %></p>
      <% else %>

        Hi!, <i class="fa fa-user"></i>

            <%= current_user.full_name.present? ? current_user.full_name : current_user.email %>
        <p>
           <%= link_to('修改個人資料', edit_registration_path(:user)) %> 
           <%= link_to("退出", destroy_user_session_path, method: :delete) %> </p>
      <% end %>
    
    <nav>
      <% if current_user %>
      <%= link_to("My Posts", account_posts_path) %>
      <%= link_to "My Portfolio", my_portfolio_path %>
    
      <% end -%>
 			<%= link_to "Articles", posts_path %>
 			<%= link_to "Projects", projects_path %>
 			<%= link_to "Contact", new_contact_path %>
 		</nav>
 	</header>

    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% elsif alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <%= yield %>
    
    <% if current_user && current_user.admin? %>
      <%= link_to "admin", admin_posts_path %>
    <% end -%>

      <footer id="main_footer">
    <p class="copyright">&copy;2017 INOceaner</p>
    <nav>
      <a href="https://twitter.com/Chris40925527" target="_blank"><i class="fa fa-twitter"></i></a>
      <a href="https://www.linkedin.com/in/chris-lyu-5a6b75a8/" target="_blank"><i class="fa fa-linkedin"></i></a>
      <a href="https://github.com/lyuchris" target="_blank"><i class="fa fa-github"></i></a>
    </nav>
  </footer>
  </body>
</html>
